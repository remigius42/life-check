## 1. Implementation (already complete)

- [x] 1.1 Add `text_sensor` with template lambda joining `daily_counts[14]` to comma-separated string
- [x] 1.2 Comment out `i2c`, `font`, and `display` blocks by default; add TTGO instructions in `docs/esp32.md`
- [x] 1.3 Set default `beam_gpio_pin` to GPIO 13 in `secrets.yaml.example`

## 2. Spec Sync

- [x] 2.1 Sync delta spec to `openspec/specs/esphome-firmware/spec.md`
