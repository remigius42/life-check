---
exclude_paths:
  - ".claude/commands/opsx/**"
  - ".claude/skills/openspec-**"
  - ".gemini/skills/openspec-**"
  - ".opencode/command/opsx-**"
  - ".opencode/skills/openspec-**"

engines:
  agentlinter:
    exclude_paths:
      # CLAUDE.md and AGENTS.md are AI instruction files, not human-facing docs.
      # AgentLinter flags false positives or offers bad advice in terms of
      # overloading the agents instructions with persona, version stamps, etc.).
      - "CLAUDE.md"
      - "AGENTS.md"
